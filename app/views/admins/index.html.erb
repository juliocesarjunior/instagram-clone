<%#= @posts.inspect %>

<div class="container">
	<div class="row">
		<div class="col-8">
			<% @posts.each do |post| %>
			<div class="bg-muted p-2"><%= post.admin.username %></div>
			<%= image_tag post.image.url, class: "img img-fluid" %>
			<% end -%>			
		</div>
	</div>
</div>